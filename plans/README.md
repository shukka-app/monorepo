# Implementation Plans

Generated by the improve skill on 2026-08-21. Scope is the `shukka/` package
(git submodule, planned at `9afada0`). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and do **not** update this index (the reviewer does).

Working directory for every plan: `/Users/akrc/Developer/shukka-monorepo/shukka`
(or `shukka/` inside a monorepo worktree). Paths in the plans are relative to
that directory.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add upload/authz/uploader characterization tests | P1 | M | — | DONE |
| 002 | Harden login, cookies, 500s, and feed decoding | P1 | M | — | DONE |
| 003 | Omit API keys from key-authenticated app detail | P1 | S | 001 | DONE |
| 004 | Make upload init/finalize and S3 errors honest | P1 | M | 001 | DONE |
| 005 | First-class Tauri publish + Action identity + notes snippet | P1 | M | — | DONE |
| 006 | Batch dashboard reads and cache feed metadata | P2 | M | 001 | DONE |
| 007 | Docs, OpenAPI, operator runbook, non-root image | P2 | M | 002, 005 | DONE |
| 008 | Regenerate lockfile so `npm ci` works | P2 | S | — | DONE |
| 009 | Spike: post-promote webhooks (design only) | P3 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- 003 and 004 extend tests introduced in 001 (`tests/app-api.test.ts`, `tests/release-flow.test.ts`). Land 001 first or the new cases collide.
- 006 changes `src/server/dashboard.ts`, which 003 also touches. Land 003 first if both run.
- 007 documents behavior from 002 (rate limit, Secure cookies, HEALTHCHECK) and 005 (Tauri Action). Can start docs-only sections earlier, but do not mark DONE until those land.
- 002, 005, 008, 009 have no code dependencies on 001.

## Findings considered and rejected

- `/api/health` missing: implemented at `src/routes/api/health.ts`.
- CI helper scripts / `run-rollback.mjs` missing: present on GitHub at `9afada0`.
- Unauthenticated feed / public notes: product contract.
- View roles as auth: documented ADR, client-only.
- Encryption key in `data/`: documented trust boundary.
- S3 endpoint SSRF: self-hosted MinIO/R2 is the point.
- CSRF tokens: `SameSite=Lax` is enough.
- Revoke all sessions on login: ADR only requires it on password change.
- Async hit writes: would break hit-trends ADR.
- Downgrade TS 6 / Zod 4: intentional stack.
- Two markdown pipelines: Milkdown edit vs remark render.
- Prettier / EditorConfig: no evidence of pain.
- Sparkle, RBAC, Prometheus, grayscale, artifact proxy: documented non-goals.
